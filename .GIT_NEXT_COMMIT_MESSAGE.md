fix(BUG8b): AI summarization CLI call and toggle-off

- Pass prompt as positional argument to `claude -p` instead of stdin
  (stdin piping was unreliable — CLI returned empty output)
- Add `--no-session-persistence` flag to avoid creating session files
- Replace invalid `spawn({ timeout })` with AbortController + signal
- Toggle OFF now calls refresh() in both VS Code and standalone handlers
- Add comprehensive test suite for SummaryService CLI invocation
