Fix 3 bugs + add sidechain activity dots to task cards

Bug fixes:
- BUG1: Filter isSidechain entries from main messages (ghost "Untitled" cards)
- BUG2: Add workspace filtering to onFileChanged (cross-project leakage)
- BUG3: Skip empty/meaningless conversations in buildConversation

New feature — sidechain activity dots:
- Collect last 3 sidechain steps during JSONL parsing (ring buffer)
- Determine step status: running/completed/failed from tool_use/tool_result
- Render colored dots (gray/green/red/yellow) in full, compact, narrow views
- Running dots pulse with 2s animation matching existing UI pattern
- Add SidechainStep type, parser collection, fingerprint diff, UI rendering
- Add tests for step collection, max 3 limit, and no-sidechain case
